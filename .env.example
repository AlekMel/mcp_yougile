# YouGile API key (Bearer token).
# Create it in the YouGile web app (Settings -> API keys) or via
# POST /api-v2/auth/keys. Required.
YOUGILE_API_KEY=

# Optional: override the API base URL (defaults to https://ru.yougile.com).
# YOUGILE_BASE_URL=https://ru.yougile.com
